import '@fontsource-variable/inter/wght.css'
import './style.css'

// The page is fully rendered at build time from config.yaml (see vite.config.ts).
// No runtime behaviour is needed; this entry only pulls in the stylesheet and
// the self-hosted Inter Variable face (bundled into dist/assets at build time).
