{
  "name": "tillhome",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "YAML-driven static personal/ops link hub",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check": "tsc && node scripts/validate-config.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "~6.0.2",
    "vite": "^8.2.2",
    "yaml": "^2.8.1"
  }
}
